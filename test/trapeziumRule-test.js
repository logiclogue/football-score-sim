const trapeziumRule = require("../src/trapeziumRule");
const expect = require("chai").expect;

describe("trapeziumRule()", () => {
    context("(0.1, 10000, x = y, 0, 0)", () => {
        it("", () => {
            expect(false).to.be.true;
        });
    });
});
