// [Team] -> Seed -> (Match -> Boolean) -> Match
function matchFinder(teams, seed, isMatch) {
    matchFinderPrime(teams, seed, isMatch, 0);
}

function matchFinderPrime(teams, seed, isMatch, index) {

}

module.exports = matchFinder;
